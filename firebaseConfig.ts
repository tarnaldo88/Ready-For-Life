// Firebase initialization for React Native
import  firebase  from '@react-native-firebase/app';

// If you need to add custom initialization logic, do it here.
// For most cases, just importing @react-native-firebase/app is enough.

export default firebase;
